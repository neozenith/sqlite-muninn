name: Publish

on:
  release:
    types: [published]
  # TEMPORARY: also run on PRs to verify build steps work
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Build All Platforms ──────────────────────────────────
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            target: linux-x86_64
            ext: so
            wheel_tag: manylinux_2_17_x86_64
          - os: ubuntu-22.04-arm
            target: linux-arm64
            ext: so
            wheel_tag: manylinux_2_17_aarch64
          - os: windows-2022
            target: windows-x86_64
            ext: dll
            wheel_tag: win_amd64
    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.target }})
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      # ── ccache (shared keys with ci.yml) ─────────────────
      - name: ccache
        if: runner.os != 'Windows'
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ${{ matrix.os }}
          evict-old-files: 1d

      # ── Unix build ──────────────────────────────────────
      - name: Build (Unix)
        if: runner.os != 'Windows'
        run: make all

      # ── Windows build ───────────────────────────────────
      - name: Setup MSVC
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Generate Windows build script
        if: runner.os == 'Windows'
        run: python3 scripts/generate_build.py windows

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: build\generated\build_windows.bat

      # ── Test (Linux only) ───────────────────────────────
      - name: Test (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get install -y libsqlite3-dev
          make test

      # ── Wheel build ─────────────────────────────────────
      - name: Copy binary into Python package
        run: cp build/muninn.${{ matrix.ext }} sqlite_muninn/
        shell: bash

      - name: Build and retag wheel
        run: |
          uv build --wheel
          uvx wheel tags --platform-tag=${{ matrix.wheel_tag }} --remove dist/*.whl
        shell: bash

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.target }}
          path: build/muninn.${{ matrix.ext }}

      - uses: actions/upload-artifact@v7
        with:
          name: wheel-${{ matrix.target }}
          path: dist/*.whl

  # ── macOS arm64 ───────────────────────────────────────
  build-macos-arm64:
    runs-on: macos-15
    name: Build (macos-arm64)
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: macos-arm64
          evict-old-files: 1d

      - name: Build extension (arm64)
        run: make ARCH=arm64 all

      - uses: actions/upload-artifact@v7
        with:
          name: macos-arm64
          path: build/muninn.dylib

  # ── macOS x86_64 ─────────────────────────────────────
  build-macos-x86_64:
    runs-on: macos-15
    name: Build (macos-x86_64)
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: macos-x86_64
          evict-old-files: 1d

      - name: Build extension (x86_64)
        run: make ARCH=x86_64 all

      - uses: actions/upload-artifact@v7
        with:
          name: macos-x86_64
          path: build/muninn.dylib

  # ── macOS Universal Binary (lipo + wheel) ────────────
  build-macos-universal:
    needs: [build-macos-arm64, build-macos-x86_64]
    runs-on: macos-15
    name: Build (macos-universal)
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - uses: actions/download-artifact@v7
        with:
          name: macos-arm64
          path: arm64/

      - uses: actions/download-artifact@v7
        with:
          name: macos-x86_64
          path: x86_64/

      - name: Create universal binary
        run: |
          mkdir -p build
          lipo -create arm64/muninn.dylib x86_64/muninn.dylib -output build/muninn.dylib
          lipo -info build/muninn.dylib

      - name: Copy binary into Python package
        run: cp build/muninn.dylib sqlite_muninn/

      - name: Build and retag wheel
        run: |
          uv build --wheel
          uvx wheel tags --platform-tag=macosx_11_0_universal2 --remove dist/*.whl

      - uses: actions/upload-artifact@v7
        with:
          name: macos-universal
          path: build/muninn.dylib

      - uses: actions/upload-artifact@v7
        with:
          name: wheel-macos-universal
          path: dist/*.whl

  # ── Amalgamation ─────────────────────────────────────────
  amalgamation:
    runs-on: ubuntu-22.04
    name: Amalgamation
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - run: make amalgamation
      - run: tar czf muninn-amalgamation.tar.gz -C dist muninn.c muninn.h

      - uses: actions/upload-artifact@v7
        with:
          name: amalgamation
          path: muninn-amalgamation.tar.gz

  # ── WASM Build ───────────────────────────────────────────
  wasm-build:
    runs-on: ubuntu-22.04
    name: WASM Build
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v14

      - name: Build WASM (lite, no llama.cpp)
        run: make build-wasm-lite

      - uses: actions/upload-artifact@v7
        with:
          name: wasm
          path: |
            build/wasm/muninn_sqlite3.js
            build/wasm/muninn_sqlite3.wasm

  # ── BLOCKED: Publish jobs (uncomment when build jobs verified) ──

  # publish-pypi:
  #   needs: [build, build-macos-universal]
  #   runs-on: ubuntu-latest
  #   name: Publish to PyPI
  #   if: github.event_name == 'release'
  #   environment: pypi
  #   permissions:
  #     id-token: write
  #   steps:
  #     - uses: actions/download-artifact@v7
  #       with:
  #         pattern: wheel-*
  #         merge-multiple: true
  #         path: dist/
  #
  #     - uses: astral-sh/setup-uv@v7
  #
  #     - name: Publish to PyPI
  #       run: uv publish dist/*.whl

  # publish-npm:
  #   needs: [build, build-macos-universal, wasm-build]
  #   runs-on: ubuntu-latest
  #   name: Publish to NPM
  #   if: github.event_name == 'release'
  #   permissions:
  #     contents: read
  #     id-token: write
  #   steps:
  #     - uses: actions/checkout@v6
  #
  #     - uses: actions/download-artifact@v7
  #
  #     - uses: actions/setup-node@v6
  #       with:
  #         node-version: "22"
  #         registry-url: https://registry.npmjs.org
  #
  #     - name: Upgrade npm for Trusted Publishing
  #       run: npm install -g npm@latest
  #
  #     - name: Determine npm dist-tag
  #       id: npm-tag
  #       run: |
  #         VERSION=$(cat VERSION)
  #         if echo "$VERSION" | grep -q "alpha"; then
  #           echo "tag=alpha" >> "$GITHUB_OUTPUT"
  #         elif echo "$VERSION" | grep -q "beta"; then
  #           echo "tag=beta" >> "$GITHUB_OUTPUT"
  #         elif echo "$VERSION" | grep -q "rc"; then
  #           echo "tag=rc" >> "$GITHUB_OUTPUT"
  #         else
  #           echo "tag=latest" >> "$GITHUB_OUTPUT"
  #         fi
  #
  #     - name: Generate npm sub-packages
  #       run: python3 scripts/generate_build.py npm
  #
  #     - name: Copy binaries into platform packages
  #       run: |
  #         cp linux-x86_64/muninn.so    dist/nodejs/platforms/linux-x64/muninn.so
  #         cp linux-arm64/muninn.so     dist/nodejs/platforms/linux-arm64/muninn.so
  #         cp windows-x86_64/muninn.dll dist/nodejs/platforms/win32-x64/muninn.dll
  #         cp macos-universal/muninn.dylib dist/nodejs/platforms/darwin-arm64/muninn.dylib
  #         cp macos-universal/muninn.dylib dist/nodejs/platforms/darwin-x64/muninn.dylib
  #
  #     - name: Publish darwin-arm64
  #       run: npm publish dist/nodejs/platforms/darwin-arm64 --provenance --access public --tag ${{ steps.npm-tag.outputs.tag }}
  #
  #     - name: Publish darwin-x64
  #       run: npm publish dist/nodejs/platforms/darwin-x64 --provenance --access public --tag ${{ steps.npm-tag.outputs.tag }}
  #
  #     - name: Publish linux-x64
  #       run: npm publish dist/nodejs/platforms/linux-x64 --provenance --access public --tag ${{ steps.npm-tag.outputs.tag }}
  #
  #     - name: Publish linux-arm64
  #       run: npm publish dist/nodejs/platforms/linux-arm64 --provenance --access public --tag ${{ steps.npm-tag.outputs.tag }}
  #
  #     - name: Publish win32-x64
  #       run: npm publish dist/nodejs/platforms/win32-x64 --provenance --access public --tag ${{ steps.npm-tag.outputs.tag }}
  #
  #     - name: Install main package dependencies
  #       working-directory: npm
  #       run: npm install
  #
  #     - name: Publish main package
  #       run: npm publish ./npm --provenance --access public --tag ${{ steps.npm-tag.outputs.tag }}
  #
  #     - name: Copy WASM artifacts into package
  #       run: |
  #         cp wasm/muninn_sqlite3.js dist/nodejs/platforms/wasm/muninn_sqlite3.js
  #         cp wasm/muninn_sqlite3.wasm dist/nodejs/platforms/wasm/muninn_sqlite3.wasm
  #
  #     - name: Publish WASM package
  #       run: npm publish dist/nodejs/platforms/wasm --provenance --access public --tag ${{ steps.npm-tag.outputs.tag }}

  # upload-release:
  #   needs: [build, build-macos-universal, amalgamation, wasm-build]
  #   runs-on: ubuntu-latest
  #   name: Upload Release Assets
  #   if: github.event_name == 'release'
  #   permissions:
  #     contents: write
  #   steps:
  #     - uses: actions/checkout@v6
  #
  #     - uses: actions/download-artifact@v7
  #
  #     - name: Rename artifacts for unique asset names
  #       run: |
  #         mv linux-x86_64/muninn.so muninn-linux-x86_64.so
  #         mv linux-arm64/muninn.so muninn-linux-arm64.so
  #         mv macos-universal/muninn.dylib muninn-macos-universal.dylib
  #         mv windows-x86_64/muninn.dll muninn-windows-x86_64.dll
  #
  #     - name: Upload to GitHub Release
  #       run: |
  #         gh release upload ${{ github.event.release.tag_name }} \
  #           muninn-linux-x86_64.so \
  #           muninn-linux-arm64.so \
  #           muninn-macos-universal.dylib \
  #           muninn-windows-x86_64.dll \
  #           amalgamation/muninn-amalgamation.tar.gz \
  #           wasm/muninn_sqlite3.js \
  #           wasm/muninn_sqlite3.wasm
  #       env:
  #         GH_TOKEN: ${{ github.token }}
