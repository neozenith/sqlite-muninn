# This project is a C library — pyproject.toml is used only for Python tooling deps.
[project]
name = "sqlite-vec-graph-docs"
version = "0.0.0"
requires-python = ">=3.12"

[dependency-groups]
docs = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
]
